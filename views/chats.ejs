<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chats</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8 font-sans">
    <div class="max-w-4xl mx-auto">
        <h3 class="text-3xl font-bold text-gray-800 mb-6 text-center">All Chats 💬</h3>
        
        <div class="flex justify-end mb-6">
            <form method="get" action="/chats/new">
                <button type="submit" class="bg-blue-600 text-white font-semibold py-2 px-6 rounded-full shadow-md hover:bg-blue-700 transition-colors duration-200">
                    New Chat 
                </button>
            </form>
        </div>

        <div class="space-y-6">
            <% for( let chat of chats ) { %>
                <div class="bg-white p-6 rounded-xl shadow-lg border border-gray-200">
                    <div class="text-gray-700 mb-2">
                        <span class="font-bold text-gray-900">From:</span> 
                        <span class="text-blue-600 font-medium"><%= chat.from %></span>
                    </div>
                    <div class="text-gray-700 mb-2">
                        <span class="font-bold text-gray-900">To:</span> 
                        <span class="text-green-600 font-medium"><%= chat.to %></span>
                    </div>
                    <div class="text-gray-800 border-l-4 border-gray-400 pl-4 py-2 mt-4">
                        <span class="font-bold text-gray-900">Message:</span> <%= chat.msg %>
                    </div>
                    <div class="text-left text-sm text-gray-500 mt-4">
                        <p>Sent on: <%= chat.created_At.toString().split(" ").slice(0,4).join(" ") %></p>
                        <p>at: <%= chat.created_At.toString().split(" ")[4] %></p>
                    </div>
                    
                    <div class="flex space-x-4 mt-6">
                        <form method="GET" action="/chats/<%= chat._id %>/edit">
                            <button type="submit" class="bg-blue-500 text-white font-bold py-2 px-4 rounded-full transition-colors duration-300 hover:bg-blue-700">
                                Edit
                            </button>
                        </form>
                        
                        <form method="POST" action="/chat/<%= chat._id %>?_method=DELETE">
                            <button type="submit" class="bg-red-500 text-white font-bold py-2 px-4 rounded-full transition-colors duration-300 hover:bg-red-700">
                                Delete
                            </button>
                        </form>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
