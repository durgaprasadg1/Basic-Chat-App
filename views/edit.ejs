<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
        <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-lg w-full">
        <h2 class="text-3xl font-bold text-center mb-2 text-gray-800">Edit Chat</h2>
        <p class="text-sm text-center mb-6 text-gray-500">
            Chat ID: <span class="font-mono text-gray-600"><%= chat.id %></span>
        </p>

        <div class="bg-gray-50 p-4 rounded-md mb-6">
            <p class="text-gray-700">
                <span class="font-semibold">From:</span> 
                <span class="text-blue-600 font-medium"><%= chat.from %></span>
            </p>
            <p class="text-gray-700">
                <span class="font-semibold">To:</span> 
                <span class="text-green-600 font-medium"><%= chat.to %></span>
            </p>
        </div>

        <form method="post" action="/chat/<%=chat.id%>?_method=PUT">
            <label for="msg" class="block text-gray-700 font-medium mb-1">Message</label>
            <textarea name="msg" id="msg" rows="6" class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none">
                <%= chat.msg %>
            </textarea>

            <div class="mt-6 text-center">
                <button type="submit" class="bg-blue-600 text-white p-3 rounded-lg font-semibold hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors duration-200 w-full">
                    Update Message
                </button>
            </div>
        </form>
    </div>
</body>
</html>

